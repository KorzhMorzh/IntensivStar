<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content"
    android:layout_width="@dimen/actor_size"
    android:layout_height="wrap_content"
    android:layout_marginEnd="@dimen/material_margin_large"
    android:layout_marginBottom="@dimen/material_margin_normal"
    android:orientation="vertical"
    tools:background="@color/blueDark"
    tools:ignore="RtlHardcoded">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/actor_image_preview"
        android:layout_width="@dimen/actor_size"
        android:layout_height="@dimen/actor_size"
        android:scaleType="centerCrop"
        app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.CornerSize49Percent"
        tools:background="@color/white"
        android:layout_gravity="center"
        tools:src="@drawable/ic_avatar" />

    <TextView
        android:id="@+id/actor_name"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/actor_size"
        android:layout_marginTop="@dimen/material_margin_small"
        android:gravity="center"
        style="@style/AppTheme.Text.TextGreyParagraph"
        tools:text="Jason Momoa" />
</LinearLayout>